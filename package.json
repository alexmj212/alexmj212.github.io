{
  "name": "alexmj212.dev",
  "version": "4.0.0",
  "description": "Alex Johnson - Portfolio & Blog",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind-input.css -o ./assets/css/main.css --minify",
    "build": "npm run build:css && bundle exec jekyll build",
    "serve": "npm run build:css && bundle exec jekyll serve --livereload",
    "dev": "npm run build:css && bundle exec jekyll serve --livereload --drafts"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "cssnano": "^7.1.0",
    "postcss-import": "^16.1.1",
    "tailwindcss": "^3.4.17"
  }
}
