---
phase: 03-accessibility-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/skills/Skills.tsx
  - src/components/experience/Experience.tsx
  - src/components/Footer.tsx
  - src/components/ThreeBackground.tsx
autonomous: true

must_haves:
  truths:
    - "Page has exactly one h1 element (hero title)"
    - "Heading hierarchy is strict: h1 > h2 > h3, no skipped levels"
    - "Three.js canvas is hidden from screen readers"
    - "Main landmark wraps primary content"
  artifacts:
    - path: "src/App.tsx"
      provides: "Single h1, h2 section titles, main landmark, header landmark"
      contains: "main id=\"main-content\""
    - path: "src/components/ThreeBackground.tsx"
      provides: "aria-hidden canvas, role=presentation container"
      contains: "aria-hidden"
    - path: "src/components/Footer.tsx"
      provides: "h2 instead of h1 for footer heading"
  key_links:
    - from: "src/App.tsx"
      to: "heading hierarchy"
      via: "h1 only in hero, h2 for sections"
      pattern: "section-title"
    - from: "src/components/ThreeBackground.tsx"
      to: "accessibility tree"
      via: "aria-hidden removes canvas from AT"
      pattern: "aria-hidden.*true"
---

<objective>
Fix semantic HTML structure and ARIA landmarks across the site to establish WCAG 2.1 AA compliant document structure.

Purpose: Correct heading hierarchy (currently 6 h1 elements, must be single h1), add proper landmarks (main, header), and hide decorative Three.js canvas from assistive technology. This is the structural foundation all other accessibility work builds on.

Output: Semantically correct HTML structure with single h1, strict heading hierarchy, proper landmarks, and aria-hidden canvas.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-accessibility-compliance/03-CONTEXT.md
@.planning/phases/03-accessibility-compliance/03-RESEARCH.md

Key source files:
@src/App.tsx
@src/components/skills/Skills.tsx
@src/components/experience/Experience.tsx
@src/components/Footer.tsx
@src/components/ThreeBackground.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix heading hierarchy and add landmarks</name>
  <files>
    src/App.tsx
    src/components/skills/Skills.tsx
    src/components/experience/Experience.tsx
    src/components/Footer.tsx
  </files>
  <action>
Fix the heading hierarchy across the site. Currently there are 6 h1 elements (hero, about, portfolio, skills, experience, footer). WCAG requires single h1 per page.

In src/App.tsx:
- Keep h1 on line 29 "Hey, I'm AJ" (this is the page title)
- Change h1 on line 53 "About Me" (section-title class) to h2
- The Portfolio component renders its own h1 internally -- that will be fixed below
- Wrap the app-container content div with proper landmarks:
  - Add `<header>` landmark around the hero section (the div#home)
  - Add `<main id="main-content">` landmark wrapping about, portfolio, skills, experience sections (from div#about through div#experience)
  - Footer component already renders a `<footer>` tag -- this is correct

In src/components/skills/Skills.tsx:
- Change h1 on line 12 (section-title class) "Skills" to h2

In src/components/experience/Experience.tsx:
- Change h1 on line 8 (section-title class) "Experience" to h2

In src/components/Footer.tsx:
- Change h1 on line 9 "Alex Johnson" to h2 (footer is not the primary heading)

IMPORTANT: Do NOT change any CSS classes. The section-title class controls visual styling. Only change the HTML element from h1 to h2. The visual appearance stays identical because section-title defines the font size via CSS, not the heading element.

Also fix heading levels inside the portfolio dialog (src/components/portfoilo/Portfolio.tsx is handled in Plan 02, do NOT touch it here).
  </action>
  <verify>
Run: grep -rn '<h1' src/App.tsx src/components/skills/Skills.tsx src/components/experience/Experience.tsx src/components/Footer.tsx

Expected: Only ONE h1 element total (in App.tsx hero section). All section titles should be h2.

Then run: grep -n 'main\|header\|footer' src/App.tsx | head -20

Expected: main and header landmarks present in App.tsx structure.
  </verify>
  <done>
Single h1 "Hey, I'm AJ" in hero section. All section titles (About Me, Portfolio, Skills, Experience) are h2. Footer heading is h2. Main landmark wraps content sections. Header landmark wraps hero.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add aria-hidden to Three.js canvas and reduced motion support</name>
  <files>
    src/components/ThreeBackground.tsx
  </files>
  <action>
The Three.js canvas is purely decorative. Screen readers should skip it entirely.

In src/components/ThreeBackground.tsx, update the return JSX (around line 414-438):

1. Add `role="presentation"` and `aria-label="Decorative background animation"` to the outer div (id="three-background")
2. Add `aria-hidden="true"` to the canvas element

The JSX should look like:
```tsx
return (
  <div
    id="three-background"
    role="presentation"
    aria-label="Decorative background animation"
    style={{...existing styles...}}
  >
    <canvas
      ref={canvasRef}
      id="hero-canvas"
      aria-hidden="true"
      style={{...existing styles...}}
    />
  </div>
);
```

Do NOT change any other code in ThreeBackground.tsx (animation logic, cleanup, etc. is correct from Phase 1).
  </action>
  <verify>
Run: grep -n 'aria-hidden\|role="presentation"\|aria-label="Decorative' src/components/ThreeBackground.tsx

Expected: aria-hidden="true" on canvas, role="presentation" on container div, aria-label on container div.
  </verify>
  <done>
Three.js canvas has aria-hidden="true", container has role="presentation" and descriptive aria-label. Screen readers will skip the decorative background entirely.
  </done>
</task>

</tasks>

<verification>
1. Run `grep -rn '<h1' src/` to confirm only one h1 across the entire src directory (the hero title). Note: Portfolio h1 will be fixed in Plan 02.
2. Run `grep -n 'aria-hidden' src/components/ThreeBackground.tsx` to confirm canvas is hidden from AT.
3. Run `npx tsc --noEmit` to confirm TypeScript still compiles cleanly.
4. Run `npm run build` to confirm build succeeds.
</verification>

<success_criteria>
- Single h1 element in hero section across App.tsx, Skills.tsx, Experience.tsx, Footer.tsx
- Section titles use h2 with existing section-title CSS class (visual appearance unchanged)
- Main landmark wraps content sections in App.tsx
- Header landmark wraps hero section in App.tsx
- Three.js canvas has aria-hidden="true"
- Three.js container has role="presentation"
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-accessibility-compliance/03-01-SUMMARY.md`
</output>
