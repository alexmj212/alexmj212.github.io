---
phase: 02-testing-infrastructure-coverage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vite-env.d.ts
  - src/components/ThreeBackground.memory.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TypeScript compiles without errors (tsc --noEmit passes)"
    - "gc() global is typed for test files using V8 --expose-gc flag"
    - "REQUIREMENTS.md MEM-03 accurately reflects implementation (5 cycles, <5MB threshold)"
  artifacts:
    - path: "src/vite-env.d.ts"
      provides: "gc() type declaration for V8 garbage collection"
      contains: "declare function gc"
    - path: "src/components/ThreeBackground.memory.test.tsx"
      provides: "Memory test with proper gc() type usage"
      contains: "typeof gc ==="
  key_links:
    - from: "src/components/ThreeBackground.memory.test.tsx"
      to: "src/vite-env.d.ts"
      via: "TypeScript global type resolution"
      pattern: "typeof gc === 'function'"
---

<objective>
Close 2 verification gaps from Phase 2: TypeScript compilation errors and requirement/implementation discrepancy.

Purpose: Achieve clean TypeScript compilation required for CI/CD and update requirements to reflect actual implementation.
Output: Zero tsc errors, accurate MEM-03 requirement definition.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-testing-infrastructure-coverage/02-VERIFICATION.md

# Prior work context
@src/components/ThreeBackground.memory.test.tsx
@src/vite-env.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gc() global type declaration and fix memory test usage</name>
  <files>src/vite-env.d.ts, src/components/ThreeBackground.memory.test.tsx</files>
  <action>
Add a global type declaration for the V8 gc() function used in memory tests.

The gc() function is available when Node/Chrome is run with --expose-gc flag. Vitest browser mode with Chromium can expose this via browser launch args.

**Step 1:** Add to src/vite-env.d.ts:
```typescript
// V8 garbage collection function (available with --expose-gc flag)
// Used in memory leak tests to force garbage collection before heap measurement
declare function gc(): void
```

The declaration uses `declare function` at module scope, which extends the global namespace when the file contains module markers (/// reference or import/export).

**Step 2:** Verify the memory test file uses proper type guard pattern:
The existing code should already have `typeof gc === 'function'` guards. If TypeScript still complains, the type declaration will resolve it.

Verify:
1. Run `npx tsc --noEmit` - should pass with zero errors
2. Run `grep 'typeof gc ===' src/components/ThreeBackground.memory.test.tsx` - should find matches confirming gc() usage guards exist
  </action>
  <verify>
1. npx tsc --noEmit exits with code 0 (no errors)
2. grep 'typeof gc ===' src/components/ThreeBackground.memory.test.tsx returns matches (confirming gc() guard pattern exists)
3. npx tsc --noEmit src/components/ThreeBackground.memory.test.tsx passes (specific file type-checks)
  </verify>
  <done>All gc() TypeScript errors resolved, clean compilation, gc() usage guards confirmed in memory test</done>
</task>

<task type="auto">
  <name>Task 2: Update MEM-03 requirement to match implementation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update MEM-03 to reflect the actual implementation and reasoning:

Current (line 14, incorrect):
```
- [ ] **MEM-03**: Memory profiling tests verify no leaks after 100 component mount/unmount cycles
```

Updated (accurate):
```
- [x] **MEM-03**: Memory profiling tests verify <5MB heap growth after 5 mount/unmount cycles
```

Key changes:
1. Change from 100 cycles to 5 cycles (as specified in CONTEXT.md and implemented)
2. Add explicit heap growth threshold (<5MB) that the test enforces
3. Mark as [x] since this requirement is now satisfied by ThreeBackground.memory.test.tsx

Why 5 cycles instead of 100:
- Phase 2 CONTEXT.md specified 5 cycles for test environment
- 5 cycles is sufficient to detect leaks (production fix achieved 0.60 MB growth)
- 100 cycles would take ~2 minutes per test run (each cycle needs 800ms for init/cleanup)
- 5 cycles provides clear signal while maintaining reasonable test speed

Why <5MB threshold:
- CONTEXT.md specified <5MB as threshold (more lenient than Phase 1's <1MB for test environment variance)
- Production achieved 0.60 MB, test environment shows higher variance
- <5MB catches genuine leaks while allowing for test environment overhead
  </action>
  <verify>grep "MEM-03" .planning/REQUIREMENTS.md shows: "- [x] **MEM-03**: Memory profiling tests verify <5MB heap growth after 5 mount/unmount cycles"</verify>
  <done>MEM-03 accurately describes implementation (5 cycles, <5MB threshold), marked as satisfied</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` passes with zero errors
2. gc() usage confirmed: `grep 'typeof gc ===' src/components/ThreeBackground.memory.test.tsx` finds matches
3. Memory tests still pass: `pnpm test:browser ThreeBackground.memory.test.tsx`
4. REQUIREMENTS.md updated: MEM-03 shows 5 cycles, <5MB threshold, marked [x]
</verification>

<success_criteria>
- tsc --noEmit exits with code 0
- All 56 tests still pass (47 unit + 9 browser)
- MEM-03 requirement updated with 5 cycles AND <5MB threshold, marked satisfied
- VERIFICATION.md gaps addressed (gc type, requirement accuracy)
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing-infrastructure-coverage/02-05-SUMMARY.md`
</output>
